# PNPM/NPM configuration for MindfulSpace monorepo

# Ignore strict peer dependencies mismatches (useful for React/ESLint plugins)
strict-peer-dependencies=false

# Automatically install missing peer dependencies (less friction in dev)
auto-install-peers=true

# Optional: use the public npm registry
registry=https://registry.npmjs.org/

# Optional: use global shared pnpm store (faster installs)
store-dir=~/.pnpm-store
